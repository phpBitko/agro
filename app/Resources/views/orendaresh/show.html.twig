{% extends 'base.html.twig' %}

{% block body %}
    <h1>OrendaResh</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orendaResh.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ orendaResh.note }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('orendaresh_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('orendaresh_edit', { 'id': orendaResh.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
